[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter @farmagest/shared build && pnpm --filter @farmagest/web build"

[deploy]
startCommand = "pnpm --filter @farmagest/web start"
restartPolicyType = "ON_FAILURE"
